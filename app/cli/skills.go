package cli

// builtinSkills maps skill directory name → SKILL.md content.
// These are always written on startup (overwriting any previous version).
// User customisations should be made in a separate fork of the skill directory.
var builtinSkills = map[string]string{
	"评审项目提示词生成": skillPromptGen,
}

// skillPromptGen is SKILL.md for the 评审项目提示词生成 skill (v3.1).
// v3.0 changes:
//   - Added output format anchoring at the top (输出铁律) for stronger constraint
//   - Step 3: changed from Agent sub-agents to direct Read for speed
//   - Step 5: stronger output prohibition with fallback rules
//   - Added ⛔ output prohibition section at the bottom
// v3.1 changes:
//   - Step 3: split read-depth by taskType; Bug 修复类不再受 2 文件上限
//   - New Step 3.5: Bug 定位与复现路径确认（调用链通读 / 候选清单 / 优先级排序 / 可复现性强校验）
//   - Step 5: 新增 Bug 修复类额外自检，防止"像 bug 但代码里不成立"的题目
const skillPromptGen = `---
name: 评审项目提示词生成
description: 根据评审项目代码仓库自动生成评测提示词（未归类/Bug修复/代码生成/Feature迭代/代码理解/代码重构/工程化）
---

# 评审项目提示词生成

## ⚠️ 输出铁律（最高优先级，贯穿全流程）

你的最终响应有且只有一个裸 JSON 对象。之前、之后、之外不允许有任何文字。
所有分析、推理、自检在内部完成，绝不输出给用户。
如果你发现自己开始写分析文字，立刻停下，只输出 JSON。
禁止输出任何 Markdown 标题、表格、代码块、分析段落。

## 参数预填充模式（PINRU 调用时使用）

当消息中包含 [PINRU] 标记时，直接从消息中解析以下参数，跳过 AskUserQuestion：

taskType / constraints / scope / notes（可选）

否则使用 AskUserQuestion 一次性问三个问题。

## 执行步骤

### Step 1：解析参数

[PINRU] 模式直接解析，非 PINRU 模式用 AskUserQuestion。

### Step 2：读取执行手册

根据 taskType 读取对应手册（必须完整阅读）：
- 未归类 → 不读取，直接结合仓库现状挑选
- Bug修复 → {{MANUAL_DIR}}/01_Bug修复任务执行手册.md
- 代码生成 → {{MANUAL_DIR}}/02_代码生成任务执行手册.md
- Feature迭代 → {{MANUAL_DIR}}/03_Feature迭代任务执行手册.md
- 代码理解 → {{MANUAL_DIR}}/04_代码理解任务执行手册.md
- 代码重构 → {{MANUAL_DIR}}/05_代码重构任务执行手册.md
- 工程化 → {{MANUAL_DIR}}/06_工程化任务执行手册.md

### Step 3：快速扫描代码仓库

当前目录即为代码仓库。

1. 运行一次 find 获取目录结构：
find . -maxdepth 3 -not -path '*/.git/*' -not -path '*/node_modules/*' -not -path '*/__pycache__/*' | sort

2. 根据 taskType 决定读码深度：
   - 非 Bug 修复类：从目录结构判断最核心的 2 个业务文件，Read 顺序读取。
   - Bug 修复类：进入 Step 3.5，按调用链通读，不受 2 个文件上限限制。

注意：Step 3 及 Step 3.5 的所有分析结果仅供内部使用，不要在最终响应中输出任何分析内容。

### Step 3.5：Bug 定位与复现路径确认（仅 taskType = Bug 修复时执行）

目标：**先在代码里找到真实存在的 bug，再翻译成用户语言**，而不是先想一个像 bug 的故事再套到项目上。

1. 按"疑似 bug 关注点 → 相关调用链完整读完"的顺序读码：
   - 先挑一个聚焦点（例如某个用户动作、某个表单、某个状态持久化）。
   - 把该关注点涉及的**所有文件**都 Read 完整：入口视图/组件 + 相关 store/状态管理 + 相关 API 层 + 路由守卫/拦截器 + 涉及的后端处理（如果在仓内）。
   - 禁止只读 1-2 个文件就下结论。跨文件交互（store 清理、路由守卫、全局拦截器）是 bug 高发区，必须看全。

2. 产出内部 bug 候选清单（不输出），每个候选必须能填完以下四项：
   - 触发路径：用户最少几步操作能触发（例如"输错密码 5 次 → 登出 → 刷新"）
   - 涉及代码：文件名 + 行号 + 相关函数
   - 根因：代码为什么会错
   - 可观察现象：用户在 UI/接口返回上能直接看到什么异常

3. 从候选里挑一条出题，**优先级排序**：
   - 触发路径 ≤ 3 步用户操作
   - 现象直观可见（UI 变化、报错、数据异常），不需要打开 devtools 才发现
   - 不依赖特殊前置状态（session 过期、脏 localStorage、并发等）
   - 如果所有候选都超过 3 步或依赖特殊前置状态 → 选最短那个，并且题目的"触发场景"必须把前置动作**逐步写全**，不得省略

4. 可复现性强校验（内部自问，任一不通过则换 bug 或改写触发场景）：
   - 新克隆仓库、干净环境下，按题目里写的步骤能不能看到题目里写的现象？
   - 题目描述的"当前行为"是不是代码真实行为？（不是想当然的 bug 故事）
   - 模型只读题目、不看我的分析，能不能按题目定位到同一块代码？

### Step 4：生成提示词

根据手册规范、代码分析、用户选择和 notes，生成一道评测提示词。

铁律（违反即重新生成）：
- 只写业务需求，读起来像产品经理在说话
- 零技术标识：文件路径、类名、函数名、变量名一律不出现
- 零 Markdown：无标题、无粗体、无代码块、无列表符号
- 极简短：正文 2-4 句
- 字数限制：全文不超过 80 字（空白字符不计入）
- 约束融入正文：所有约束要求（技术栈、架构、代码风格、业务规则等）必须作为正文的自然组成部分写出，和需求描述合在同一段里，像开发者在聊天窗口一口气说完的话。严禁将约束单独分段、分行或加任何前缀标签。严禁出现"xx约束：""xx约束:""xx要求："等"标签名称：内容"形式的分类标头。若无约束则不加任何约束相关句子
- 修改范围：scope 覆盖面体现在题目的功能范围里
- 去 AI 味：
  1. 禁用词：此外、同时、另外、彰显、培养、突出、展示、至关重要、格局、持久、不可或缺、兜底、根因、收口、顺手、再顺一遍、收尾、落库
  2. 禁用结构："需要 X""实现 X 功能""不仅……而且……""确保……""以便……"
  3. 禁用三段式列举
  4. 禁止过度限定："可能""或许""一定程度上"
  5. 句子节奏长短交错
  6. 读感测试：像真人抱怨问题就过，像说明书就重写

正面参考示例（约束融入正文，一整段写完）：
公益岗位有合同到期时间，现在系统只能查看所有在职记录，没法提前看到哪些合同快到期了。在公益岗位模块加一个到期预警视图，列出30天内要到期的岗位和已过期未退出的人员，标明剩余天数。使用现有桌面端 IPC 通信方式对接数据，不引入新的第三方包。查询逻辑放在主进程，渲染进程只负责展示，保持现有进程分工。对外函数需标注参数和返回类型，变量名采用小驼峰格式。

反面示例（严禁这样写——约束单独分行加标签前缀）：
❌ ...标明剩余天数。
技术栈约束：使用现有桌面端 IPC 通信方式对接数据，不引入新的第三方包。
架构约束：查询逻辑放在主进程，渲染进程只负责展示，保持现有进程分工。
代码风格约束：对外函数需标注参数和返回类型，变量名采用小驼峰格式。

### Step 5：自检、写入、结束

**自检**（内部完成，不写进响应）：字数、技术标识、禁用词、读感。

**Bug 修复类额外自检**（任一不通过必须重写）：
- 题目里的"当前行为"是不是代码里真实存在的行为，而不是猜测？
- 题目里的"触发场景"是不是最短复现步骤的 1:1 翻译，前置动作（登录、登出、过期、刷新、切页）写全了吗？
- 被测模型按题目去代码里找，能不能定位到我在 Step 3.5 选定的那段代码？

**写入**：用 Write 工具把提示词正文写入当前目录的 任务提示词.md。

**结束**：你的最终响应只能是这个裸 JSON：
{"version":1,"prompt":"提示词全文","artifactPath":"任务提示词.md绝对路径","fileWritten":true}

## ⛔ 输出禁令（违反即判定失败，无例外）

你调用完所有工具后，发送给用户的最终文本响应必须且只能是上面那个 JSON 对象。

绝对禁止：
- Markdown 标题（# ## ###）、表格、代码块
- JSON 前的任何文字（"生成结果"、"提示词如下"、"现在我已经"、"让我"、"基于"）
- JSON 后的任何补充
- 任何分析性内容（Bug 原因、涉及范围、架构说明）
- 以"我"开头的任何句子

**最后**：不确定是否该输出某段文字，就不要输出。宁可只有 JSON，不可多一个字。
`
